-#.row
-#  .col-sm-12
-#    %dl
-#      =@test.name
-#      =@test.chair
-#      =@test.course
-#      -if @test.cover_file_name!=nil
-#        =markdown(File.read(@test.cover_file_name))
-#
-#
-#      -#=raw(@test.cover.url)
-#      -#=markdown(@test.cover.url)
-#-if session[:user] == 'tutor'
-#
-#  = link_to 'Редактировать', edit_test_path(@test),class: 'btn btn-primary'
-#  \|
-#  = link_to 'Назад', tests_path,class: 'btn btn-primary'



%p
  %b=Test.human_attribute_name(:name)
  = @test.name

%p
  %b=Test.human_attribute_name(:content)+':'
  = markdown(@test.content)

  -if @test.cover_file_name!=nil
    %p
      %b='Загруженный тест'
      = markdown(File.read(@test.cover_file_name))
-if session[:user] != 'stud1' and session[:user] != 'stud2'
  = link_to 'Редактирование', edit_test_path(@test),class: 'btn btn-primary'
  \|
  = link_to 'Назад', tests_path,class: 'btn btn-primary'