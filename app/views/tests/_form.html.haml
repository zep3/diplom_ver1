-#= form_for @test do |f|
-#  - if @test.errors.any?
-#    #error_explanation
-#      %h2= "#{pluralize(@test.errors.count, "error")} prohibited this test from being saved:"
-#      %ul
-#        - @test.errors.full_messages.each do |msg|
-#          %li= msg
-#
-#  .field
-#    = f.label :name
-#    = f.text_field :name
-#  .field
-#    = f.label :chair
-#    = f.text_field :chair
-#  .field
-#    = f.label :course
-#    = f.number_field :course
-#  .field
-#    = f.label :group
-#    = f.text_field :group
-#  .field
-#    = f.label :content
-#    = f.text_area :content
-#  .field
-#    = f.label :title
-#    = f.text_field :title
-#  .actions
-#    = f.submit 'Save'

-if session[:user] != 'stud1' and session[:user] != 'stud2'
  = form_for @test do |f|
    =error_messages_for @test
    .form-group
      =f.label :name
      =f.text_field :name, class: 'form-control'
    .form-group
      =f.label :content
      =f.text_area :content, class: 'form-control'
    .form-group
      = f.label :topic_id
      = f.select :topic_id, Topic.all.map{|c| [c.name, c.id]},{}
    .form-group
      =f.label :cover
      =f.file_field :cover

      -#%li{class: ('active' if controller_name=='genres')}=link_to "Жанры",markdown_preview_url

    .form-actions
    = f.submit nil, class: 'btn btn-primary'
-else
  %h1
    Не доступно